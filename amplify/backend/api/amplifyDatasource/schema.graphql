type ItemInList @model @auth(rules: [{allow: public}]) {
  id: ID!
  Item: Item @hasOne
  List: ShoppingList @hasOne
}

type Item @model @auth(rules: [{allow: public}]) {
  id: ID!
  label: String
}

type ShoppingList @model @auth(rules: [{allow: public}]) {
  id: ID!
  label: String
}
